//#include "hashmap.h"
//#include "stack.h"
//
//int AIMakeMove(struct hashmap* board) {
//	int move, x = board->size, y = getY(board);
//	getScore(board);
//	return move;
//}
//
//int getScore(struct hashmap* board) { // determines the best column to make a play in by giving each a score based on their current state
//	// horizontal check
//	for()
//}